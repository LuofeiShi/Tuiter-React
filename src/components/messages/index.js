/**
 * @file contains the message tab component
 */
import React from "react";

/**
 * message tab on the home screen
 * @returns {JSX.Element} content of message tab
 * @constructor messages constructor
 */
const Messages = () => {
  return(
    <h1>Messages Screen</h1>
  );
};
export default Messages;