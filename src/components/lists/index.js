/**
 * @file contains the component of lists
 */
import React from "react";

/**
 * the list component on the screen
 * @returns {JSX.Element} content of list
 * @constructor constructor of List
 */
const Lists = () => {
  return(
    <h1>Lists Screen</h1>
  );
};
export default Lists;